services:
  frontend:
    image: deanalemao/mern-frontend-employee:latest
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - mern-network

  backend:
    image: deanalemao/mern-backend-employee:latest
    expose:
      - "5001"
    env_file:
      - ./backend/.env
    depends_on:
      - mongo
    networks:
      - mern-network

  mongo:
    image: mongo:7.0
    expose: 
      - "27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - mern-network

networks:
  mern-network:
    driver: bridge

volumes:
  mongo-data:
    driver: local
